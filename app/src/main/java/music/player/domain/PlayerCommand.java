package music.player.domain;

public enum PlayerCommand {
    PLAY,
    PAUSE,
    NEXT,
    PREVIOUS,
    RESTART,
    SEEK_FORWARD,
    SEEK_BACKWARD,
    SHUFFLE_TOGGLE,
    REPEAT_TOGGLE,
    MUTE
}
